import React from 'react';
import MintWrapper from './MintWrapper';
import MintBtns from './MintBtns';
import { btnObjs, mintWrapper, mintRow, mintCol } from './MintStyle';

import styled from 'styled-components';






const Mint = () => {
  return (
        <MintWrapper/>
  );
};

export default Mint;
